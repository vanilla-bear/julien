{% if label_hidden %}  
    {% for item in items %}
	    <div class="col-md-6">
			<div class="row">
	        	<div{{ attributes.addClass('fes2-img equal-height navi-bg-image') }} data-bg-image="{{ item.content['#item'].entity.uri.value|image_create_url }}"></div>
	        </div>
		</div>
    {% endfor %}
{% endif %}
	</div>
</div>
